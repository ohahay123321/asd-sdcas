// Assume this is a simplified version of the original script.js
// Fixing the like button class name and adding delete functionality

const likeButton = document.querySelector('.like-btn-sketch');

function likePost() {
    // code to like a post
}

function deletePost() {
    // code to delete the post
}

likeButton.addEventListener('click', likePost);

// Assuming there's a delete button with class 'delete-btn'
const deleteButton = document.querySelector('.delete-btn');
deleteButton.addEventListener('click', deletePost);